/*----------------------------------------------------------------------------------------------------------------------------
	nrmain2
		program to give simple wrapper for nrmain2
----------------------------------------------------------------------------------------------------------------------------*/

#include <cstdlib>
#include <iostream>
#include <iomanip>
#include <string>
#include <vector>
#include "nrmain_fn.h"

using namespace std;

/*----------------------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------------------
	CONTENTS
		1 - defining inputs to main2_fn
		2 - running main2_fn
----------------------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------------------*/

int main(int argc, char** argv) {
/*----------------------------------------------------------------------------------------------------------------------------
	1. defining inputs to main_fn
----------------------------------------------------------------------------------------------------------------------------*/
vector <string> argv_main((uint)argc);
for (uint j=0; j<(uint)argc; j++) {
	argv_main[j] = (string)argv[j];
}

/*----------------------------------------------------------------------------------------------------------------------------
	2. running main_fn
----------------------------------------------------------------------------------------------------------------------------*/
int returnValue;
returnValue = nrmain2_fn(argc,argv_main);

if (returnValue!=0) {
	cerr << "return " << returnValue << " on running main" << endl;
}

return 0;
}
